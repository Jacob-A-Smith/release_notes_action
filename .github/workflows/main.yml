on: [push]

jobs:
    release_gen_job:
        runs-on: ubuntu-latest
        name: A job to generate release notes
        steps:
            - name: Checkout
              uses: actions/checkout@v2
            - name: git log step
              id: gitlog
              run: |
                git log --pretty=format:"{%h, %t, %p  - %an, %ae, %ad: %B}" > log.txt
              shell: bash
            - name: generate notes
              id: gennotes
              uses: ./
              with: 
                ./.github/workflows/log.txt